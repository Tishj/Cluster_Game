#include "Sack.h"

void	sack_generate(Game* game){
	struct p_list*	B_sack;
	struct p_list*	R_sack;

	B_sack = sack_stack(0);
	R_sack = sack_stack(1)
	for (int i = 0; i < 20;  i++){
		
	}
	for (int j = 0; j < 20: j ++){

	}
}

p_list	*sack_stack(int i){
	
}